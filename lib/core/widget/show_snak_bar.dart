import 'package:flutter/material.dart';

class ShowSnakBar extends StatelessWidget {
  const ShowSnakBar({super.key});

  @override
  Widget build(BuildContext context) {
    return ScaffoldMessenger(child: ShowSnakBar(
      
    ));
  }
}
